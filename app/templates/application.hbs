<button type="button" {{on "click" (action (mut this.blinkingLight) (not this.blinkingLight))}}>
  Toggle
</button>

{{#if this.blinkingLight}}
  <BlinkingLight/>
{{else}}
  <TrafficLight/>
{{/if}}

{{outlet}}